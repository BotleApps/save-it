name: Deploy Web to Vercel

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: 18
          cache: npm

      - name: Install dependencies
        run: npm install --legacy-peer-deps

      - name: Export Expo web bundle
        env:
          EXPO_ROUTER_APP_ROOT: app
        run: npx expo export --platform web --output-dir dist

      - name: Deploy to Vercel
        env:
          VERCEL_TOKEN: ${{ secrets.VERCEL_TOKEN }}
          VERCEL_PROJECT_ID: ${{ secrets.VERCEL_PROJECT_ID }}
          VERCEL_ORG_ID: ${{ secrets.VERCEL_ORG_ID }}
          VERCEL_SCOPE: ${{ secrets.VERCEL_SCOPE }}
        shell: bash
        run: |
          if [[ -z "$VERCEL_TOKEN" || -z "$VERCEL_PROJECT_ID" || -z "$VERCEL_ORG_ID" ]]; then
            echo "VERCEL_TOKEN, VERCEL_PROJECT_ID, and VERCEL_ORG_ID must be set" >&2
            exit 1
          fi

          args=(dist --prod --yes --token "$VERCEL_TOKEN")

          if [[ -n "$VERCEL_SCOPE" ]]; then
            args+=(--scope "$VERCEL_SCOPE")
          fi

          export VERCEL_PROJECT_ID
          export VERCEL_ORG_ID

          npx vercel deploy "${args[@]}"
